import { Board, Players } from "./cachedData.ts"

export const getBoardData = async () => {
  try {
    
  } catch (e) {
    console.log(e)
    throw e
  }
}

// After board data is pulled, update cached board data
const updateCachedBoard = () => {
  try {
    
  } catch (e) {
    console.log(e)
    throw e
  }
}

// Called when team data is gotten
// Passed in all data for that team, will calc points based on cached board data
export const calcPoints = () => {
  try {
    
  } catch (e) {
    console.log(e)
    throw e
  }
}
