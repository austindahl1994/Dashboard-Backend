import { EmbedBuilder } from "discord.js";
import { recurring } from "../../videogames/osrs/cachedBounty.js"

export const recurringEmbed = () => {
  try {
    
  } catch (e) {
    console.error(`Error creating recurring bounties embed: ${error}`);
  }
}

export const emptyRecurringEmbed = () => {
  try {
    
  } catch (e) {
    console.error(`Error creating empty recurring embed: ${error}`);
  }
}

export const completedRecurring = () => {
  try {
    
  } catch (e) {
    console.error(`Error creating completed recurring embed: ${error}`);
  }
}
