import { Board, Players } from "./cachedData.ts"

// Gets player data from sheets
export const getPlayerData = async () => {
  try {
    
  } catch (e) {
    console.log(e)
    throw e
  }
}

// Updates player data from buyins
export const updatePlayerData = async () => {
  try {
    
  } catch (e) {
    console.log(e)
    throw e
  }
}

// After player data is obtained from sheets, cache it
export const updateCachedPlayers = () => {
  try {
    
  } catch (e) {
    console.log(e)
    throw e
  }
}

// Gets player data by discord_id
export const getPlayer = () => {
  try {
    
  } catch (e) {
    console.log(e)
    throw e
  }
}
